# ============================================================
# Переменные окружения для docker-compose
# ============================================================

# URL бэкенда (для фронтенда)
# В браузере открывается localhost:5000, поэтому указываем localhost
VITE_BACKEND_URL=http://localhost:5000

# Если нужно изменить порт бэкенда:
# VITE_BACKEND_URL=http://localhost:8000
